[package]
name = "http-routing"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
helper = { path = "../../helper", default-features = false }
spin-sdk = "2.2.0"
