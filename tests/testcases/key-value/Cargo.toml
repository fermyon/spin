[package]
name = "key-value"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
http = "0.2"
itertools = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_qs = "0.12"
spin-sdk = { path = "../../../sdk/rust" }

[workspace]
