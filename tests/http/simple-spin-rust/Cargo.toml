[package]
name    = "simple-spin-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = [ "cdylib" ]

[dependencies]
anyhow = "1"
bytes = "1"
http = "0.2"
spin-sdk = { path = "../../../sdk/rust"}
wit-bindgen = "0.6"

[workspace]

# Metadata about this component.
[package.metadata.component]
name = "spinhelloworld"
