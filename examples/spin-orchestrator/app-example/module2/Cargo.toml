[package]
name = "module2-orchestrator-app-example"
authors = ["suneet nangia <suneetnangia@gmail.com>"]
description = "Guest wasm module which is orchestrated by spin orchestrator plugin."
version = "0.1.0"
edition = "2021"

[lib]
crate-type = [ "cdylib" ]

[dependencies]
wit-bindgen = "0.6.0"

[workspace]
