spin_manifest_version = "1"
authors = ["Suneet Nangia <suneetnangia@gmail.com>"]
description = ""
name = ""
trigger = { type = "http", base = "/" }
version = "0.1.0"

[[component]]
id = "mqtt-outbound-test"
source = "target/wasm32-wasi/release/mqtt-outbound-test.wasm"
allowed_http_hosts = []
[component.trigger]
route = "/..."
[component.build]
command = "cargo build --target wasm32-wasi --release"
watch = ["src/**/*.rs", "Cargo.toml"]
