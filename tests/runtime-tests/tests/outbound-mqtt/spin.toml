spin_manifest_version = 2

[application]
name = "outbound-mqtt"
authors = ["Suneet Nangia <suneetnangia@gmail.com>"]
version = "0.1.0"

[[trigger.http]]
route = "/"
component = "test"

[component.test]
source = "%{source=outbound-mqtt}"
environment = { MQTT_ADDRESS = "mqtt://localhost:%{port=1883}" }
allowed_outbound_hosts = ["mqtt://localhost:%{port=1883}"]
