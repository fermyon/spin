[package]
name = "internal-http-streaming-back-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
futures = "0.3.30"
helper = { path = "../../helper", default-features = false }
spin-sdk = "2.2"
